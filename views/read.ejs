<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>mongo coode</title>
    <link rel="stylesheet" href="/stylesheet/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
   

</head>
<body>
    <div class="w-full h-full bg-zinc-900 p-10  text-white">

          <div class=" text-xl  text-blue-500 tracking-tighter mb-5 ">
            <a href="/">back to home</a> </div>

        <h1 class="text-2xl tracking-tighter mb-3 ">All users</h1>

        <div class="users flex gap-3 flex-wrap">
           

            <% users.forEach(function(user){%>
                <div class="user w-72 p-4 bg-zinc-800 rounded-lg" type="text">
                    <div  class="w-full h-72 rounded-lg bg-zinc-500">
                        <img class="w-full h-full object-cover" src="<%= user.image%>" alt=""></div>
                    <h3 class="text-xl tracking-tighter mt-3"><%= user.name%></h3>
                    <h3 class="text-xl  text-zinc-600 tracking-tighter mt-3"><%= user.email%></h3>
       
                    <div class="flex gap-3 mt-10  ">
                       <a class="text-blue-700" href="/edit/<%= user._id%>">Edit user </a>
                       <a class="text-red-700" href="/delete/<%= user._id%>">Delete user</a>
                    </div>
               </div>
            <% })%>
           
     
           
       

       
        </div>
    </div>
</body>
</html>